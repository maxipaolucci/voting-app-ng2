import './helloWorld.scss';

export const helloWorldStr = "This is the hello world component.";

//this fn is just defined to test the mocka testing library (see test directory)
export const squareFn = function (value = 0) {
  return value * value;
};